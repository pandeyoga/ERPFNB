# HANDOFF — Torado Group ERP (Aurora F&B)
**Sesi:** 2026-07-18 · **Bahasa:** Indonesia
**Preview URL:** `https://repo-sync-tour.preview.emergentagent.com`
**Login:** `admin@torado.id` / `Torado@2026` (SUPER_ADMIN; lihat `memory/test_credentials.md` untuk 10 seed users)

---

## 1) TL;DR — Di mana kita sekarang

| Fase | Status | Detail singkat |
|---|---|---|
| **Restore repo** | ✅ | Clone `pandekomangyogaswastika-dot/torado61` ke `/app`, deps terpasang, services hijau |
| **Tour & Guide validasi (commit terakhir)** | ✅ | `audit_tours[_v2]`: 0 missing / 0 drift · 429 target resolve — TETAP HIJAU |
| **PHASE 1 — IDOR sweep** | ✅ | 7 P0 IDOR di-fix (leave×4 + uploads×3); probe extended 2→8 kasus, semua PASS |
| **PHASE 2 — Route-level RBAC 7 portal** | ✅ | `portalGate.js` + Gate di Finance/HR/Inv/Outlet/Procurement/Owner/Executive; testing agent 43/43 URL-tamper PASS |
| **PHASE 4 — E2E WRITE flows** | ✅ | `e2e_write_flows.py` 4/4 PASS (PR approval, payment JE, period lock, inventory transfer); Gate 7 di forensic suite |
| **PHASE 3 — Browser tour render sweep** | ✅ **DONE (2026-07-18 continuation)** | Probe **FAIL=0 · 100% healthy** (121 PASS / 29 PARTIAL, 4× deterministic). 13 FAIL routes fixed: tourMap route mismatches, real `/admin/approvals` routing bug (ApprovalMatrixBuilder was shadowed by MyApprovals alias), tour target selector corrections, loading-state root anchors. Integrated as **Gate 8**. |
| **PHASE 5 — Deploy gate** | ✅ **READY** | `deployment_agent` = PASS (fixed `.gitignore` .env block). `forensic_master_suite.sh` = **8/8 PASS**. Ready to click Deploy. |

**Baseline gates saat ini (SEMUA HIJAU — Phase 3 tuntas):**
- `forensic_master_suite.sh` = **8/8 PASS** (Gate 8 baru = tour runtime render probe, headless Playwright)
- `tour_render_probe.py` = **FAIL=0 · 100% healthy** (121 PASS / 29 PARTIAL)
- `pytest` = 233 pass / 11 skip
- `verify_data_integrity` = 21/21 PASS
- `health_check` = 25/25 PASS
- `idor_ownership_probe` = 8/8 no leak
- `audit_tours[_v2]` = 0 static drift

---

## 2) PHASE 3 — Status detail (harus dilanjut)

### 2.1 Alat yang sudah dibuat
| File | Fungsi | Status |
|---|---|---|
| `/app/scripts/dump_tour_map.py` | Parse `tourMap.js` + `tours/*.js` → JSON `(route, tour_id, [targets])` | ✅ Ready |
| `/app/scripts/artifacts/tour_map_expanded.json` | 186 route→tour pairs, 649 step targets | ✅ Generated (juga di-refresh via `dump_tour_map.py` kalau tourMap.js berubah) |
| `/app/scripts/tour_render_probe.py` | Playwright: login → navigate ke setiap route → verify target di DOM. Session-loss aware (re-login otomatis). Smart-wait per selector. | ✅ Ready |
| `/app/scripts/artifacts/tour_render_report.json` | Report per-route: verdict + found + missing selectors | ✅ Generated |
| `/app/scripts/artifacts/tour_probe.log` | Log full jalannya probe terakhir | ✅ |

### 2.2 Cara jalankan ulang probe (di lokal container)
```bash
# 1. Refresh mapping kalau tourMap.js/tours/*.js berubah
python /app/scripts/dump_tour_map.py

# 2. Jalankan probe (butuh ~5 menit; jalan background disarankan)
cd /app && nohup python scripts/tour_render_probe.py > /app/scripts/artifacts/tour_probe.log 2>&1 &
disown

# 3. Pantau
tail -f /app/scripts/artifacts/tour_probe.log

# 4. Cek verdict setelah selesai
python -c "
import json
r = json.load(open('/app/scripts/artifacts/tour_render_report.json'))
print(r['verdict_counts'])
print(f'health = {(r[\"verdict_counts\"][\"PASS\"]+r[\"verdict_counts\"][\"PARTIAL\"])/r[\"static_testable\"]:.1%}')
"
```
Gate exit code 0 kalau `FAIL=0 AND healthy>=90%`. Saat ini `FAIL=23 healthy=86%` → GATE FAIL.

### 2.3 Fix yang SUDAH dilakukan sesi ini (di `frontend/src/contexts/tour/tourMap.js`)
9 mapping stale dihapus (route yang mengarah ke tour dengan testid-target di halaman lain):

```
✂ /admin/loyalty/analytics    (targets loyalty HOME)
✂ /admin/tax                  (duplicate of tax-config)
✂ /procurement/pr/new         (form ≠ list)
✂ /procurement/po/new         (form ≠ list)
✂ /procurement/gr/new         (form ≠ list)
✂ /procurement/rfq/new        (form ≠ list)
✂ /finance/budget/manage      (form ≠ hub)
✂ /finance/tax-center         (legacy TaxCenter ≠ hub)
✂ /finance/payments/new       (form ≠ list)
✂ /finance/payment-requests/new (form ≠ list)
✂ /executive/ai               (route doesn't exist)
```
Sudah diverifikasi: `audit_tours[_v2].py` tetap 0 drift setelah penghapusan.

### 2.4 Fix yang MASIH TERSISA (23 FAIL routes)

**Kategori A — Testid mismatch component vs tour (butuh edit component/tour):**

| Route | Tour ID | Target expected | Yang ada di halaman |
|---|---|---|---|
| `/admin/audit-log` | `admin-audit-log` | `admin-page`, `admin-master-tiles`, `admin-welcome` (SALAH — ini testids admin HOME) | `audit-log-page`, `audit-toolbar`, `audit-filter-entity`, `audit-refresh`, `audit-table-card` |
| `/admin/integrations` | `admin-integrations` | `page`, `header`, `tabs` (terlalu generik, tidak ada) | `email-test-to`, `llm-test-prompt`, `wa-test-to` |
| `/admin/tax-config` | `admin-tax-config` | `tax-config-page`, `tax-cards-grid`, `tax-config-info-banner` | Perlu dicek — kemungkinan halaman render `<TaxConfig>` yang belum punya testid page-level |
| `/executive/brand-mix` | `executive-brand-mix` | `brand-mix-overview` (OK), `brand-mix-period` (TIDAK ADA) | Punya `brand-mix-overview` + `brand-mix-card-*` (dynamic) |
| `/finance/ar` | `finance-ar` | `ar-invoice-list`, `ar-kpi-strip`, `ar-tabs`, `create-invoice-btn` | Testids ADA di `ARInvoiceList.jsx` — mungkin route `/finance/ar` tidak resolve ke halaman ini (Portal cuma punya `/finance/ar-invoices`). **CEK: apakah `/finance/ar` masih di tourMap.js? Bila iya, ganti ke `/finance/ar-invoices` atau hapus.** |
| `/finance/periods` | `finance-periods` | `period-lock-btn`, `periods-page`, `periods-table` (SALAH) | `period-year`, `period-refresh`, `period-lock-{period}` (dynamic per row) |
| `/inventory/adjustments` | `inventory-adjustment` | `inventory-adjustment-page`, `adj-outlet-select`, `adj-new-btn`, `adj-table` | `adj-new` (bukan `adj-new-btn`), `adj-state-{id}` (dynamic) — **tidak ada testid page-level** |
| `/outlet/reservations` | `outlet-reservation-list` | `filters`, `table-wrap` (generik, tidak ada) | Cuma ada `reschedule-*` (modal-scope). **ReservationList.jsx belum punya testid page-level** |
| `/outlet/reservations/new` | `outlet-reservation-form` | `actions`, `datetime-card`, `deposit-card`, `outlet-card`, `page` | `reservation-form-page`, `reservation-form-outlet-card`, `reservation-form-datetime-card`, `reservation-form-guest-card` — **prefix beda** |
| `/owner/ai-assistant` | `owner-ai-assistant` | `executive-qa-page`, `qa-input`, `qa-suggestion` (dari ExecutiveQA) | Halaman render `<ConversationalQA>` (bukan ExecutiveQA) — testids beda |

**Kategori B — Loading state (butuh timeout lebih panjang atau state fallback):**
- `/admin/approval-builder` dan `/admin/approvals` — page-nya `if (loading) return <div>Memuat builder…</div>`. Fetch 5 endpoints (roles, outlets, brands, users, entity_types). **Solusi**: (a) tambahkan `data-testid="approval-matrix-builder-loading"` di div loading agar probe stopper punya sesuatu untuk di-wait, ATAU (b) tingkatkan probe smart-wait timeout dari 1600ms → 5000ms untuk halaman ini.
- `/admin/effective-dating` — timeline data async; halaman punya testids target yang benar, cuma butuh wait lebih.

**Rekomendasi urutan fix untuk sesi berikutnya:**

1. **Fix component-side (paling aman, tidak break tour)** — tambahkan testid yang hilang:
   - `ReservationList.jsx` → wrap root: `<div data-testid="reservation-list-page">`, filter section: `data-testid="reservation-list-filters"`, table: `data-testid="reservation-list-table"`
   - `AdjustmentList.jsx` → wrap root: `data-testid="inventory-adjustment-page"`, filter/outlet section: `data-testid="adj-outlet-select"`, table: `data-testid="adj-table"` (button `adj-new` sudah ada — update tour: `adj-new-btn` → `adj-new`)
   - `AuditLog.jsx` → update **tour** `admin-audit-log` di `tours/admin.js` untuk pakai `audit-log-page`, `audit-toolbar`, `audit-filter-entity`, `audit-table-card` (jangan pakai admin home testids)
   - `TaxConfig.jsx` → tambah `data-testid="tax-config-page"` di root, `data-testid="tax-cards-grid"` di grid, `data-testid="tax-config-info-banner"` di banner
   - `Integrations` hub → tambah `data-testid="admin-integrations-page"`, `data-testid="admin-integrations-tabs"`, DAN update tour dari `page`/`header`/`tabs` ke prefix `admin-integrations-*`
   - `BrandMixOverview.jsx` → tambah `data-testid="brand-mix-period"` ke period selector
   - `PeriodList.jsx` → tambah `data-testid="periods-page"` root, `data-testid="periods-table"` table. Tour target `period-lock-btn` → ganti ke `period-refresh` (button umum, selalu render)
   - `ReservationForm.jsx` → tour perlu diupdate: `page` → `reservation-form-page`, `outlet-card` → `reservation-form-outlet-card`, `datetime-card` → `reservation-form-datetime-card`, dst. **Prefix `reservation-form-` sudah dipakai di komponen**
   - `ConversationalQA.jsx` (dipakai OwnerPortal `/ai-assistant`) → tambah testid `executive-qa-page` (alias) supaya reuse `owner-ai-assistant` tour (yang aliased ke ExecutiveQA tour) tetap valid. **Alternatif**: bikin `owner-ai-assistant` tour terpisah dengan target ConversationalQA-native.

2. **Fix loading state** — tambah `data-testid="approval-matrix-builder-loading"` di komponen ApprovalMatrixBuilder, lalu di `tours/admin.js` step pertama sebaiknya target root container (yang selalu ada) atau tingkatkan wait di probe.

3. **Fix routing** — cek apakah `/finance/ar` masih ada di tourMap.js. Bila iya, hapus (route sebenarnya `/finance/ar-invoices`).

4. **Fix wrong-tour aliases** — di `tours/registry.js`, beberapa tour ID punya alias ke tour lain (contoh `admin-audit-log: adminHomeTour`). Cek yang masih relevan setelah fix di atas.

---

## 3) File yang PERLU DISENTUH (path lengkap)

**Component-side (tambah/perbaiki data-testid):**
- `/app/frontend/src/portals/outlet/ReservationList.jsx`
- `/app/frontend/src/portals/outlet/ReservationForm.jsx`
- `/app/frontend/src/portals/inventory/AdjustmentList.jsx`
- `/app/frontend/src/portals/admin/AuditLog.jsx`
- `/app/frontend/src/portals/admin/tax/*` (cari TaxConfig)
- `/app/frontend/src/portals/admin/integrations/*` (cari halaman index)
- `/app/frontend/src/portals/executive/BrandMixOverview.jsx`
- `/app/frontend/src/portals/finance/PeriodList.jsx`
- `/app/frontend/src/portals/admin/ApprovalMatrixBuilder/index.jsx` (tambah loading testid)
- `/app/frontend/src/components/shared/ConversationalQA.jsx` (tambah testid alias)

**Tour-side (update target selectors):**
- `/app/frontend/src/contexts/tour/tours/admin.js` — `adminAuditLogTour` (baru bikin, jangan alias adminHomeTour), `adminIntegrationsTour`, `adminTaxConfigTour`
- `/app/frontend/src/contexts/tour/tours/executive.js` — `executiveBrandMixTour` (tambah step target `brand-mix-period`)
- `/app/frontend/src/contexts/tour/tours/finance.js` — `financePeriodsTour` (ganti target ke `period-refresh` + `periods-table`)
- `/app/frontend/src/contexts/tour/tours/inventory.js` — `inventoryAdjustmentTour` (target `adj-new-btn` → `adj-new`)
- `/app/frontend/src/contexts/tour/tours/outlet.js` — `outletReservationListTour`, `outletReservationFormTour` (ganti target ke `reservation-list-*` / `reservation-form-*`)
- `/app/frontend/src/contexts/tour/tours/registry.js` — putus alias `admin-audit-log: adminHomeTour` (bikin dedicated `adminAuditLogTour`)

**Verifikasi setelah setiap batch fix:**
```bash
# 1. Cek static drift tetap 0 (wajib!)
python /app/scripts/audit_tours.py       # 0 missing / 0 drift
python /app/scripts/audit_tours_v2.py    # 0 MISSING

# 2. Refresh mapping dan jalankan probe
python /app/scripts/dump_tour_map.py
python /app/scripts/tour_render_probe.py
# Target: FAIL=0, healthy>=90%
```

---

## 4) Kalau PHASE 3 selesai → PHASE 5 (Deploy)

```bash
# 1. Pastikan forensic suite masih 7/7 hijau + probe hijau
bash /app/scripts/forensic_master_suite.sh
python /app/scripts/tour_render_probe.py

# 2. Integrasikan probe sebagai Gate 8 di forensic_master_suite.sh
#    (append di bawah Gate 7):
#    gate "Tour render probe (Phase 3)" python tour_render_probe.py

# 3. Panggil deployment_agent
```
Panggil `deployment_agent` dengan konteks bahwa Phase 1-4 sudah hijau dan Phase 3 baru saja diselesaikan.

---

## 5) Snapshot penting untuk sesi berikutnya

### 5.1 Timeline PART Q/R/S di ground truth
Lokasi: `/app/memory/GROUND_TRUTH_2026-06-17.md` lines 521-746. Baca dari **PART Q** ke bawah untuk konteks penuh sesi 2026-07-18.

### 5.2 Test credentials
Lokasi: `/app/memory/test_credentials.md` — 10 seed users, semua password `Torado@2026`.

### 5.3 Files paling penting untuk load context di sesi berikutnya
- `/app/plan.md` — plan makro + status per fase
- `/app/memory/GROUND_TRUTH_2026-06-17.md` — history lengkap PART A-S
- `/app/memory/ENGINEERING_GUARDRAILS.md` — aturan SSOT (jangan langgar)
- `/app/scripts/artifacts/tour_render_report.json` — data mentah 164 route hasil probe
- **Handoff ini** (`/app/HANDOFF_2026-07-18.md`)

### 5.4 Load context script (jalankan di awal sesi)
```bash
bash /app/scripts/load_context.sh --no-probe
```
Otomatis print status, seed check, dan link ke tier-0 docs.

### 5.5 Reseed database (opsional, kalau perlu reset)
```bash
echo "" | bash /app/scripts/seed_reset.sh
```

---

## 6) Command cepat untuk lanjut sesi berikutnya

```bash
# 0. Cek service hidup
sudo supervisorctl status | grep -E "backend|frontend"

# 1. Cek gates masih hijau
cd /app && bash scripts/forensic_master_suite.sh 2>&1 | tail -12

# 2. Cek Phase 3 status
python -c "
import json
r = json.load(open('/app/scripts/artifacts/tour_render_report.json'))
c = r['verdict_counts']
print(f'PASS={c[\"PASS\"]}  PARTIAL={c[\"PARTIAL\"]}  FAIL={c[\"FAIL\"]}')
"

# 3. Baca daftar FAIL untuk targeting
python -c "
import json
r = json.load(open('/app/scripts/artifacts/tour_render_report.json'))
fails = [x for x in r['results'] if x['verdict'] == 'FAIL']
for x in fails:
    print(f\"{x['route']:<45} tours={x['tours']}  missing={x['missing'][:2]}\")
"

# 4. Mulai fix, lalu re-run probe
python /app/scripts/dump_tour_map.py
python /app/scripts/tour_render_probe.py
```

---

## 7) Catatan penting

- **JANGAN** ubah `.env` files (`/app/backend/.env`, `/app/frontend/.env`) — aturan hard.
- **JANGAN** ubah nama testid yang sudah dipakai tour lain — cek dulu apakah testid tersebut direferensi tour lain via `grep -rn "'testid-name'" /app/frontend/src/contexts/tour/tours/`.
- **Prioritas**: fix Phase 3 sampai `FAIL=0 healthy>=90%` → integrasi Gate 8 → jalankan `deployment_agent` → deploy.
- **Estimasi effort Phase 3 sisa**: 1-2 jam untuk 23 route (mostly copy-paste testid tambahan atau update target di tour steps).
- **JANGAN skip**: setelah setiap batch fix, jalankan `audit_tours[_v2].py` untuk pastikan static drift tetap 0.

---

**Sesi berakhir dengan:**
- forensic_master_suite: 7/7 PASS
- Phase 3 probe: 86% healthy (target 90%, FAIL 23 tersisa)
- Semua kode Phase 1/2/4 sudah production-ready dan diverifikasi
- Handoff siap untuk continuation seamless
